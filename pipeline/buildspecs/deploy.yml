version: 0.2

phases:

  install:
    runtime-versions:
      nodejs:24
    commands:
      - corepack enable
      - npm install -g aws-cdk
  build:
    commands:
      - cdk deploy --app cdk.out --require-approval never -c env=$DEPLOY_ENV