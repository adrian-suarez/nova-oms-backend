# Store de pnpm fijado a una ruta relativa del proyecto (en vez del default fuera del
# workspace, que varía según la imagen/usuario) — permite que el caché local de CodeBuild
# (PipelineConstruct.ts) apunte a una ruta conocida y determinística entre corridas.
store-dir=.pnpm-store
